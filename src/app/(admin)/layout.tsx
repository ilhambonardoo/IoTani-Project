const layoutAdmin = ({ children }: { children: React.ReactNode }) => {
  return (
    <div className="px-24  h-screen from-stone-600 to-stone-100 bg-gradient-to-b">
      {children}
    </div>
  );
};

export default layoutAdmin;
